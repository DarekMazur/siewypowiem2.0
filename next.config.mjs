/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatars.githubusercontent.com',
      'cloudflare-ipfs.com',
      'loremflickr.com',
      'picsum.photos',
    ],
  },
};

export default nextConfig;
