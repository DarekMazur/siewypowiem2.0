import { StyledArticleDate } from './ArticleDate.styles';

const ArticleDate = ({ date }: { date: string }) => {
  return <StyledArticleDate>{date}</StyledArticleDate>;
};

export default ArticleDate;
