import styled from 'styled-components';
import { Handwritting } from '../Handwritting/Handwritting.styles';

export const ArticleAuthor = styled(Handwritting)`
  position: absolute;
  bottom: 1rem;
  left: 1rem;
`;
