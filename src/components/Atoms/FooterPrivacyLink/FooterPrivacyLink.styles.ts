import styled from 'styled-components';

export const StyledFooterPrivacfyLink = styled.div`
  z-index: 1;
  display: flex;
  justify-content: flex-end;
  font-size: ${({ theme }) => theme.fontSize.s};
`;
